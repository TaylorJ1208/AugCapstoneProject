export interface User {
    userId: number;
    firstName: string;
    lastName: string;
    email: string;
    userName: string;
    contact: string;
    password: string;
    roles: string;
    ssn: string;
    // Still need to add ADDRESS / ROLE / ORDER
}